using UnityEngine;

public class GBFStateMachineController : MonoBehaviour
{
    [SerializeField]
    public GBStateSOBase[] state;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
